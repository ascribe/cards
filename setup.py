#!/usr/bin/env python

from setuptools import setup

setup(
    name='cards',
    version='0.1',
    description='Server to respond to crawlers of Facebook, Twitter, etc',
    author='Troy McConaghy',
    author_email='troy@ascribe.io',
    packages=['ascribe_cards'],
)
